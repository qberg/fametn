import RootLayout from "@/pages/layout";
import { Container } from "react-bootstrap";

const TNCGS = () => {
    return (<RootLayout>
        <Container>
            hgi
        </Container>
    </RootLayout>)
}

export default TNCGS;